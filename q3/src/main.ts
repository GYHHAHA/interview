import { Component, computed, createApp, inject, provide, reactive, watchEffect } from 'vue'
import './reset.css'
import router from './router/index.js';
import VueRouter from 'vue-router';
import App from './App.vue'
ref == reactive();
computed
watch
watchEffect
v-if(
    <load>
) else {
    <Component>
}
provide inject

createApp(App).use(router).mount('#app')
